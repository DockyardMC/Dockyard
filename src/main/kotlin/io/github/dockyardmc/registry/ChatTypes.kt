package io.github.dockyardmc.registry

object ChatTypes {

    // dockyard does not use vanilla chat. Use system chat instead
    val registryCache = Registry("minecraft:chat_type", mutableListOf())

}